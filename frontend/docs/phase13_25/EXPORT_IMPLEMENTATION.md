# Phase 13.25 — Tasks Export Implementation

Export is implemented entirely in the Laravel admin panel (`TaskExportController` + `ReportExportService`). The public Next.js app has no Tasks export surface.

See `backend/docs/phase13_25/EXPORT_IMPLEMENTATION.md`.
