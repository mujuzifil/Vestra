# Filter Simplification — Phase 13.31

## Removed

**Assigned To** from:

- `EnquiriesPage` state / URL / reset / export query
- `filter-bar.blade.php`
- `EnquiryAdminService` query, filter options, sort, export
- `EnquiryExportController` filter + column

## Remaining filters

Status, Type, Priority, Source (when present), Received date range, plus header search.
