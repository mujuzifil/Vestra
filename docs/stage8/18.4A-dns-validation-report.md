# Stage 18.4A — DNS Validation Report

## DNS Record

The Project Owner configured the following DNS record:

| Type | Host | Value |
|------|------|-------|
| A | `admin` | `187.77.84.119` |

This creates `admin.vestradetergents.com` pointing to the production VPS IP.

## Validation Performed

Command:

```bash
nslookup admin.vestradetergents.com
```

Result:

```
Server:  UnKnown
Address:  192.168.100.1

Name:    admin.vestradetergents.com
Address:  187.77.84.119
```

## Resolution Status

✅ `admin.vestradetergents.com` resolves to the expected production IP `187.77.84.119`.

## Propagation Status

DNS propagation is sufficient for validation from the local resolver used during this check. Because DNS is globally distributed, final propagation to all resolvers may take additional time (typically up to the TTL of the record). No further DNS changes are required for this stage.

## Notes

- DNS management is out of scope; this stage only validates the existing record.
- The same IP as the existing `vestradetergents.com` and `api.vestradetergents.com` records is expected, since the VPS hosts all three services.
